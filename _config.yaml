# site properties and page defaults
title: Learning and Control (LC) Lab 
subtitle: 
description: By integrating reinforcement learning with control theory, we build intelligent robots that can operate safely and autonomously in complex, dynamic environments.
header: images/background.jpg
footer: images/background.jpg
proofer: false

# site social media and other links
links:
  email: lcui@unm.edu
  orcid: 0000-0001-8031-7638
  google-scholar: RbczS_gAAAAJ
  github: lc-intelligentrobots
  twitter: larobot
  youtube: learningandcontrolforintellige


### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post
  # markdown files in /_courses
  - scope:
      type: "courses"
    values:
      layout: post


collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true
  # generate page for each course
  courses:
    output: true


# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
